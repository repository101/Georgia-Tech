If an image displays on your screen you can hit 'Enter' or 'Return' to exit continue execution and close the images. 

To get my results make sure you have the images in the same directory as epsilon.py.

CMD -> python epsilon.py

This will run my script to produce my results.